# pier documentation

`pier` is a small `kubectl` wrapper that maps a friendly service name to its
Kubernetes coordinates (context + namespace, plus optional resource names) via a
config file — **targeting `staging` by default**. Start with the top-level
[`README.md`](../README.md) for the user-facing overview.

## Map

### Available now

- [Installation](installation.md) — `go install` and prebuilt binaries.
- [Design specs](design/) — the rationale behind `pier` and its subsystems.
- [Implementation plans](plans/) — the phased build plan.

### Populated with the feature stack

These pages land alongside the code they describe (see the
[documentation-infrastructure design](design/2026-07-07-pier-docs-infra-design.md)
for the branch mapping):

- **Usage** (`usage.md`) — command walkthrough — with `feat/cli-implementation`.
- **Configuration** (`configuration.md`) — config file, resolution, and
  discovery — with `feat/cli-implementation` and `feat/config-discover`.
- **Architecture** (`architecture.md`) — the thin-wrapper design and ADRs — with
  `feat/cli-implementation`.
- **Command reference** (`commands/`) — autogenerated from the cobra command
  tree — with `feat/cli-implementation`.
- **Agent usage** (`agents.md`) — JSON / `discover` modes and parseable output —
  with `feat/agent-consumable-cli`.

## Generation convention

The command reference, man pages, and shell completions are **generated** from
the cobra command tree, not written by hand. The tooling lands on
`feat/cli-implementation` (which has the command tree); the convention is:

- **Command reference:** `cobra.GenMarkdownTree` → `docs/commands/`.
- **Man pages:** `github.com/spf13/cobra/doc.GenManTree`.
- **Shell completions:** cobra's built-in `completion` command (bash / zsh /
  fish / powershell).

Documentation is plain Markdown rendered natively on GitHub — no static-site
generator and no Node toolchain. Verification is manual review plus GitHub
rendering.
